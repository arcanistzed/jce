<form class="{{cssClass}} flexcol">
	<header id="jce-sheet-header">
		<label for"jce-choose-editor">Editor Library: </label>
		<select id="jce-choose-editor">
			{{#each editors}}
			<option>{{this}}</option>
			{{/each}}
		</select>
	</header>

	<div id="jce-editor"></div>

	<footer class="jce-sheet-footer">
		<button type="submit" name="submit">
			<i class="fa fa-check"></i>
			{{localize "Save Changes"}}
		</button>
	</footer>
</form>